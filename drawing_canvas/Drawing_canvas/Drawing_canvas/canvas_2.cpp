// in c++ non è necessario creare le virtual table
// si scrie virtual davanti al metodo( davanti  a distruttore e la draw)


// Un point eredita da shape
// esistono diversi tipi di eredietarietà
// struct point:: public shape {}
// non serve scrivere il distrutture se non dobbiamo fare niente in più 
// per costruttore --> <costruttore_nome> : <nome_classe_base> con il proprio costruttore
// si utilizza override che dice che sostituisce la funzione virtuale della  classe base( ma non è del tutto richiesto farlo)
